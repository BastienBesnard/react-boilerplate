// TODO
const DOMAIN = {
    DO_LABEL: {
        component: () => {},
        validate: () => {},
        format: (value) => {
            return value;
        },
        unformat: (value) => {
            return value;
        }
    },
    DO_AMOUNT: { validate: () => {}, format: () => {}, unformat: () => {} },
    DO_BOOLEAN: { validate: () => {}, format: () => {}, unformat: () => {} }
};
